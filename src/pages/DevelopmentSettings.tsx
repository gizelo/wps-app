import { Box, Typography } from "@mui/material";

export function DevelopmentSettingsPage() {
  return (
    <Box sx={{ p: 2 }}>
      <Typography variant="h4">Development Settings</Typography>
    </Box>
  );
}
